[user]
    email = arman23b@gmail.com
    name = arman23b
[credentials]
    helper = cache --timeout=86400
[color]
    ui = true
[credential]
	helper = cache --timeout=86400
[core]
    editor = vim
[alias]
    co = checkout
    ci = commit
    st = status
    br = branch
    hist = log --pretty=format:\"%h %ad | %s%d [%an]\" --graph --date=short
    type = cat-file -t
    dump = cat-file -p
